source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

gem "delayed_job_recurring", path: "~/Rails/Gems/delayed_job_recurring"

# Specify your gem's dependencies in snip_snap_zap.gemspec
gemspec
